import AppTabs from '@/components/app-tabs';

export const unstable_settings = {
  initialRouteName: 'index',
};

/** Görünür 5 sekme. Mistik/rapor/paywall kök Stack'te — gizli NativeTabs düşmesin. */
export default AppTabs;
